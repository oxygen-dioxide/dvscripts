# dvverb
将dv文件中所有音符的歌词（除0和-）替换为ver，以用于verb音源，需要 python dvfile

## 使用方法

双击运行dvverb.py，将dv文件拖入窗口，回车，将会输出至原dv文件同一文件夹下的<源文件名>verb.dv

## 命令行
```
python dvverb.py <filename1> <filename2> <filename3> ...
```